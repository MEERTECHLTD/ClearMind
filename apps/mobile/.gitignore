# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (prebuild output — regenerated by CI / `expo prebuild`)
*.orig.*
ios/
android/

# Signing material — NEVER commit. Only credentials.example.json is tracked.
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
credentials.json
credentials/
upload-keystore.*
*.b64

# Metro
.metro-health-check*

# Dependencies
node_modules/

# Env
.env
.env*.local

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# Firebase (provided via CI secret, decoded at build time)
google-services.json
