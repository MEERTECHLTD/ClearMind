{
  "//": "Template only. CI runs `envsubst < credentials.example.json > credentials.json` and decodes the keystore into credentials/upload-keystore.jks. NEVER commit credentials.json or any *.jks — see .gitignore. `eas build --local` reads this file for release signing.",
  "android": {
    "keystore": {
      "keystorePath": "credentials/upload-keystore.jks",
      "keystorePassword": "$ANDROID_KEYSTORE_PASSWORD",
      "keyAlias": "clearmind-upload",
      "keyPassword": "$ANDROID_KEY_PASSWORD"
    }
  }
}
